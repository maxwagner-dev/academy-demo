apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-code
  namespace: old-academy
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: azurefile